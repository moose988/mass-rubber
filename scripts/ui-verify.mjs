import { chromium } from 'playwright';
const url='http://127.0.0.1:4173/index.html'; const browser=await chromium.launch(); const page=await browser.newPage({viewport:{width:1280,height:900}}); const errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.goto(url,{waitUntil:'networkidle'}); if(!await page.locator('text=Sign in').count())throw new Error('Sign-in gate not shown.');await page.fill('#email','worker@example.com');await page.fill('#password','not-a-real-password');await page.click('#login');await page.waitForTimeout(100);if(!await page.locator('#loginMessage').count())throw new Error('Authentication feedback missing.');await page.screenshot({path:'test-results/worker-desktop.png',fullPage:true});await page.setViewportSize({width:390,height:844});await page.screenshot({path:'test-results/worker-mobile.png',fullPage:true});if(errors.length)throw new Error(errors.join(' | '));await browser.close();console.log('Protected Worker Mode browser verification passed.');
